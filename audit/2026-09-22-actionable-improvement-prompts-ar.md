# برومبتات تنفيذية لرفع الحالة الحالية

**تاريخ الإعداد:** 2026-09-22  
**النطاق:** المستودع المحلي `QC-Operations-Laboratory-Management-System`، مع مشاهدة حية للقراءة فقط عند إعداد هذه القائمة.  
**خط الأساس المرجعي:** `HEAD=6c505e65f410ae7ce4384c2d314578d821a53457` وقت الصياغة. افحص `HEAD` و`git status` مجددًا قبل كل مهمة؛ لا تنقل أدلة مرشح سابق إلى مرشح جديد.  
**طريقة الاستخدام:** انسخ برومبتًا واحدًا في كل مهمة. نفّذ حسب الاعتماد المبين أدناه. لا تُعامل نسبة تقرير التحليل السابق كهدف آلي؛ التحسن يُثبت بسلوك وأدلة جديدة على المرشح نفسه.

## قواعد مشتركة لكل برومبت

اقرأ `AGENTS.md` و`.agents/mind/01-mind-latest.md` كاملًا والتعليمات المحلية، ثم افحص المصادر الحالية ذات الصلة. احفظ تغييرات المستخدم. استخرج القواعد المنظمة من `Documents/` بدل اختراع صلاحيات أو معايير علمية أو مدد احتفاظ. نفّذ محليًا وتحقق على Node `24.20.0` وفق `.nvmrc`/`package.json`. سلّم: الملفات المعدلة، السبب، الحالات الطبيعية وحالات الرفض، أوامر التحقق ونتائجها، SHA وبصمة الشجرة المتسخة إن وجدت، وما بقي `NOT VERIFIED` أو `BLOCKED`. لا تكتب أسرارًا في المخرجات. لا `commit` أو `push` أو نشر أو تعديل بيانات إنتاج أو migration إنتاج دون تفويض صريح يشمل العملية. نجاح الاختبارات لا يعني اعتماد إصدار أو UAT.

## خريطة التنفيذ

| الرمز | الأولوية | يعتمد على | النتيجة المطلوبة |
|---|---|---|---|
| P01 | P0 | — | هوية نشر ومخطط قاعدة قابلان للتحقق، وتشخيص فجوة التقارير |
| P02 | P0 | P01 | أدلة CI/DB/E2E مرتبطة بمرشح واحد وتفشل عند التقادم |
| P03 | P0 | — | إزالة احتمال كشف سر قاعدة البيانات وحد الذاكرة في النسخ |
| P04 | P0 | P03 | نسخة واستعادة معزولة مثبتتان، دون تجاوز اعتماد الإنتاج |
| P05 | P0 | P02 | سير QC مصادق وصلاحياته واختبارات الفشل من البداية للنهاية |
| P06 | P0 | قرار QC/QMS ثم P05 | إكمال مصدر قرار التفتيش والاعتماد المرحلي بدون اختراع قواعد |
| P07 | P1 | — | مسار الملفات والأدلة الآمن المدمج في التطبيق |
| P08 | P1 | — | سلامة حمولة التدقيق والسجلات الحساسة |
| P09 | P1 | — | حدود معمارية سليمة بين الصفحات والتطبيق والبنية التحتية |
| P10 | P1 | P01 | رصد الأعطال والتنبيه مع تعريفات صحة دقيقة |
| P11 | P1 | P05 | UX للجداول والطوابير والبحث وحالات الفراغ والخطأ |
| P12 | P1 | P05 وP11 | تحقق وصول واستجابة ولوحة مفاتيح على تدفقات مصادقة |
| P13 | P2 | P02 | بيانات رئيسية وجودة بيانات وأداء بسجل مصدر وقياس |
| P14 | P2 | P02 | تقييم AI الاستشاري والحوكمة وتجربة المراجعة البشرية |
| P15 | P0 للإطلاق | P01–P14 حسب الانطباق | مصالحة المتطلبات والمخاطر وبوابات الإصدار وUAT |

---

## P01 — هوية النشر وفجوة مخطط `/reject-reports`

> افحص سبب ظهور `/reject-reports` في النظام الحي برسالة أن migrations اللازمة غير مطبقة، بينما `/system/health` يعرض `READY` وDB `HEALTHY` وهوية SHA/head `UNVERIFIED`. ابدأ من `src/pages/reject-reports/index.astro` و`src/actions/reject-reports.ts` و`src/modules/reject-reports/infrastructure/postgres-repository.ts` و`src/modules/system-health/infrastructure/postgres-migration-status.ts` و`db/migrations/` و`render.yaml` و`Documents/DEPLOYMENT-ARCHITECTURE.md`. نفّذ تشخيصًا للقراءة فقط يربط نسخة التطبيق الفعلية، build/release ID، migration head المطبق، وhead المصدر؛ لا تطبع عنوان قاعدة البيانات أو أي سر. أصلح في الكود أو واجهة الصحة أي ادعاء `READY` مضلل إذا كان مسار عمل مطلوب محجوبًا بمخطط قديم، مع فصل liveness عن readiness وعن جاهزية إصدار QC. حضّر خطة ترقية forward-only قابلة للمراجعة تتضمن النسخ والاستعادة والتحقق والرجوع التشغيلي وأثر التوقف. معايير القبول: عرض سبب الفجوة وحالتها صراحة، تماثل فحص المخطط مع الاعتماد الفعلي للصفحة، وضابطة سلبية لرأس migration قديم، وتوثيق ما يحتاج موافقة المشغل. شغّل فحوص الحالة والمخطط والتكامل المناسبة على قاعدة قابلة للتلف. **لا تنفّذ migration على الإنتاج في هذه المهمة.**

## P02 — أدلة مرشح واحدة عبر CI وPostgreSQL وE2E

> راجع `.github/workflows/ci.yml` و`scripts/release/check-verification-evidence.mjs` و`scripts/verification/run-authenticated-e2e.ts` و`tests/e2e/authenticated-closure.spec.ts` و`package.json`. اجعل مخرجات unit/integration/migrations/concurrency/security/build/E2E تحمل SHA، بصمة الشجرة أو artifact digest، migration head، نسخة Node، تاريخ التوليد، وبيئة التنفيذ. ارفض تلقائيًا تقريرًا ناقصًا أو من مرشح آخر أو نتيجة فيها skips غير مبررة؛ لا تعيد وسم `.ci-results` قديمة. شغّل Postgres 18 في Testcontainers أو بيئة disposable مكافئة موثقة، واعزل fixtures وteardown لكل suite؛ عالج فشل fixture/session من جذره. معايير القبول: بوابة الأدلة تفشل عمدًا مع SHA متغير أو تقرير قديم، وتمر فقط بعد إعادة توليد كل الأدلة المطلوبة لنفس المرشح، مع أعداد pass/fail/skip صريحة. إذا تعذر Docker أو CI، سجّل `BLOCKED` للطبقة المتأثرة ولا تعوضها بنجاح typecheck أو build.

## P03 — أمن أمر النسخ الاحتياطي وحد الذاكرة

> أصلح `src/modules/backup-recovery/infrastructure/postgres-logical-backup-executor.ts`: `pg_dump` يستقبل `databaseUrl` ضمن argv، و`pg_restore --dbname` يستقبل `targetDatabaseUrl` ضمن argv؛ صمّم تمرير الاتصال بحيث لا يظهر سر محتمل في قائمة العمليات أو الأخطاء. حافظ على TLS والتحقق من هدف الاستعادة المعزول وعلى رسائل الخطأ الآمنة. أزل `readFile(output)` والنقل الكامل للـdump في الذاكرة عبر عقد streaming أو ملف مرحلي محمي وحد حجم معلوم على جميع طبقات `ports/artifact-store.ts` و`local-artifact-store.ts` و`cloudflare-r2-artifact-store.ts` حسب الحاجة؛ لا تغيّر الصيغة دون مسار توافق. أضف حالات تحقق لكلمة مرور في URL وهمي لا تظهر في args/logs، نسخة كبيرة محدودة الذاكرة، فشل وسط النقل، وتنظيف الملف المؤقت. لا تكتب بيانات اعتماد فعلية في الاختبارات أو التقرير. قِس حد الذاكرة قبل/بعد على بيانات تركيبية واذكر حجم العينة.

## P04 — إثبات النسخ والاستعادة والتعافي

> بعد P03، افحص `src/modules/backup-recovery/application/request-restore.ts` و`validate-restore-request.ts` و`scripts/recovery/run-daily-backup.ts` و`run-monthly-restore-drill.ts` و`Documents/BACKUP-RECOVERY-PLAN.md`. الواجهة الحالية تحفظ طلب الاستعادة `PLANNED` وتعيد `executed:false/NOT_AVAILABLE`؛ لا تعرضه كاستعادة ناجحة. صِل جدولة النسخ وفهرسها والتحقق من سلامة artifact باستعادة فعلية إلى قاعدة معزولة وملفات أدلة معزولة، ثم تحقق من المخطط وعدد/ترابط السجلات والبصمات والصلاحيات وقابلية تشغيل التطبيق. سجّل زمن الاستعادة وفقد البيانات المقاس وقارنه **فقط** بهدف RPO/RTO معتمد؛ أبق الهدف `POLICY-DEPENDENT` إذا لم يوجد اعتماد. معايير القبول: نسخة حديثة تظهر بالفهرس، تمرين restore ناجح قابل للإعادة، فشل واضح للنسخة التالفة/الناقصة، ودليل يربط backup ID وSHA وmigration head. أبق استعادة الإنتاج fail-closed إلى حين قرار صلاحية وتشغيل صريح؛ لا تشغّلها هنا.

## P05 — سير QC كامل بصلاحيات وسلوك فشل حقيقي

> ابنِ تحققًا مصادقًا من الاستلام إلى التفتيش والمختبر والمراجعة والاعتماد والإفراج في `tests/e2e/critical-workflows.spec.ts` و`authorization-matrix.spec.ts` و`authenticated-closure.spec.ts` مع fixtures منعزلة. استخرج انتقالات الحالات والصلاحيات وفصل الواجبات من `Documents/STATE-MACHINES.md` و`BUSINESS-RULES.md` و`ROLE-MATRIX.md` و`PERMISSION-MATRIX.md`، ولا تجعل نجاح الواجهة يساوي نجاح انتقال الخادم. غطِّ الأدوار المصرح وغير المصرح، scope، رفض الاعتماد الذاتي، التكرار، stale version، انقطاع الجلسة، والفشل بين الالتزام وإرسال الإشعار؛ أثبت أن `PASS` لا يعني `RELEASED`. افحص audit/signature/record linkage في PostgreSQL disposable بعد الخطوات. معايير القبول: حالات النجاح والرفض والتزام الحالة النهائية مثبتة لنفس SHA دون skips؛ أي قرار علمي أو سياسة غير معتمدة يبقى اختبارًا محجوبًا موثقًا لا قيمة وهمية في fixture.

## P06 — إغلاق مدخل قرار التفتيش المعتمد

> تتبع العقد المفقود لسلسلة اعتماد التفتيش في `tests/integration/qc-100-final-013/two-stage-controlled-approval.test.ts` ووحدات `src/modules/quarantine/inspection/` و`Documents/REQUIREMENTS-TRACEABILITY.md` و`Documents/DECISION-ASSUMPTION-REGISTER-026.md`. حدّد بالضبط أين يلزم مصدر نتيجة رسمي أو معيار PD-01/PD-02/PD-07 ومن يملكه في QC/QMS؛ اعرض نموذج قرار محدد الحقول ومخرجاته المطلوبة للمالك. بعد وصول قرار معتمد فقط، نفّذ create/action والتحقق الخادمي وانتقالات SUBMITTED→UNDER_REVIEW→PENDING_QCM_APPROVAL→APPROVED وفق العقد، مع فصل الدور والتوقيع ونسخة السجل والتدقيق. معايير القبول: مسار إيجابي ومجموعة رفض التفويض/المعيار/التزامن تمر على PostgreSQL disposable وE2E؛ لا تستنتج معيار قبول من الكود الحالي أو من بيانات اختبار. إن لم يصل القرار، سلّم الجزء الممكن وحالة `BLOCKED` الدقيقة بدل فتح المسار بقيمة افتراضية.

## P07 — دمج الملفات والأدلة مع سياسة نوع ملف واضحة

> `src/shared/files/file-service.ts` يملك فحوص حجم وتوقيع وتفويض، لكن تتبع استعماله في صفحات/actions لم يُظهر مسار رفع حقيقي. افحص `src/shared/files/` و`Documents/SECURITY-ARCHITECTURE.md` و`Documents/REQUIREMENTS-TRACEABILITY.md` وعقود الأدلة في الوحدات. اربط رفع/عرض/تنزيل الملف بسجل العمل المناسب مع تفويض خادمي لكل عملية، مخزن خاص، hash، ربط دليل immutable حيث يلزم، ومعالجة orphan والفشل الجزئي وidempotency. أخرج قرارًا مطلوبًا لسياسة allowlist/MIME والفحص الخبيث والاحتفاظ/الحذف إن لم تكن معتمدة؛ حافظ على الرفض الآمن حتى اعتمادها. معايير القبول: اختبار صلاحية إيجابي وسلبي، ملف تنفيذي/توقيع مزيف/oversize، مسار فشل تخزين بعد DB والعكس، تنزيل لا يكشف رابطًا عامًا أو سجلًا خارج النطاق. لا تستخدم ملف مستخدم حقيقي في الاختبارات.

## P08 — حمولة Audit متداخلة وخصوصية السجلات

> أصلح `src/shared/audit/audit-event.ts`: `assertSafeAuditPayload` يفحص أسماء المفاتيح العليا فقط؛ تتبع كتّاب audit وread models/exports المرتبطة. صمّم عقد payload مسموحًا صراحة أو تعقيمًا عميقًا محدود العمق والحجم يرفض أسماء/قيم أسرار وPII بحسب السياسة المعتمدة، بما في ذلك المصفوفات والكائنات المتداخلة. لا تسقط حقولًا لازمة لإثبات حدث منظم دون بديل موثق. طبّق حدًا موحدًا عند الكتابة وعند التصدير مع احتفاظ traceability وrequest ID بلا كشف محتوى حساس. معايير القبول: أمثلة nested password/token/authorization تفشل بأمان؛ payload صالح لا يتغير معنويًا؛ السجل المصدر والتصدير لا يسرّبان أسرارًا؛ اختبارات الرفض والحدود تعمل دون تخزين سر حقيقي.

## P09 — تصحيح حدود العمارة وتبعيات الصفحات

> نفّذ `pnpm test:architecture` وافحص `src/pages/quality/ncr/[ncrId].astro` و`src/pages/quality/capa/[capaId].astro` و`src/pages/ai-advisory.astro` وأي خرق حالي يظهره الحارس. انقل استعلامات SQL/infra والمنطق الدوميني المباشر من delivery إلى use cases/read services في الوحدات المناسبة، مع حقن dependencies عبر حدود التطبيق. لا تغيّر سلوك التفويض أو ترتيب نتائج القراءة، ولا توسّع استثناءات الحارس لإخفاء المخالفة. معايير القبول: الحارس PASS، نتائج الصفحات والصلاحيات ثابتة في اختبارات التكامل، لا imports مباشرة من `infrastructure`/`domain` المخالف للعقد داخل delivery، وdiff محدود يمكن مراجعته.

## P10 — رصد فشل قاعدة البيانات والخدمات التابعة

> راجع `src/shared/database/pool.ts` و`src/shared/observability/` و`src/shared/health/` و`src/pages/system/health.astro` و`Documents/OBSERVABILITY-ARCHITECTURE.md`. معالج `sharedPool.on('error')` يصنع `AppError` ثم يهمله؛ صِل فشل الاتصال غير المتزامن إلى telemetry/log آمن مع correlation، دون URL أو استعلام أو بيانات صفوف. فرّق في العرض والتنبيه بين liveness وreadiness وcapability degradation: storage/AI غير متاح، outbox pending أو backlog متنامٍ، backup catalog فارغ، restore غير مثبت. استخرج العتبات والجهة المستقبلة للتنبيه من قرار معتمد؛ إن غابت، وفّر المقاييس والتشخيص مع `NOT CONFIGURED` واضح. معايير القبول: حقن فشل pool وخدمة تابعة يظهر إشارة قابلة للبحث ولا يخرج سرًا؛ الصحة لا تعلن اكتمال QC أو جاهزية إصدار من probe DB وحده؛ اختبار الضوضاء/تكرار التنبيه وفق السياسة إن وجدت.

## P11 — الجداول والطوابير والبحث في سياق الأدوار

> افحص `src/ui/components/data/DataTable.astro` واستعماله الفعلي، و`src/pages/work/index.astro` و`src/pages/dashboard/index.astro` وصفحات receiving/laboratory/documents، مع `Documents/UI-UX-SPECIFICATION.md` و`Documents/ROLE-OPERATING-GUIDES.md`. اختر طابورين كثيفين بالبيانات يحتاجان تحسينًا فعليًا، ووحّد عرض الأعمدة والترتيب والترشيح والبحث والصفحات دون فرض مكون عام على شاشات لا يناسبها. يجب أن يكون الفرز والتصفية في الخادم حيث يلزم، وأن يحترما scope؛ فرّق بين صفر حقيقي و`NOT_AUTHORIZED` و`NOT_SUPPLIED` والخطأ. اعرض سبب الحجز والفعل التالي والمالك والرابط الحقيقي عندما يقدمه read model، لا تقدر البيانات الناقصة. معايير القبول: بقاء عدد العناصر ونتائج البحث متسقة عبر paging، سلوك فراغ/تحميل/خطأ، روابط صفوف مصرح بها فقط، وقياس زمن إنجاز مهمتين قبل/بعد على بيانات تركيبية. لا تدّع تحسن UX بلا قياس.

## P12 — وصول وتجربة مصادقة قابلة للتحقق

> طبق خطة تحقق على `tests/e2e/accessibility.spec.ts` وصفحات login/dashboard/work/receiving/lab review/dialogs مع `Documents/DESIGN-SYSTEM.md` و`UI-UX-SPECIFICATION.md`. اختبر لوحة المفاتيح فقط، ترتيب وعودة التركيز، تسميات النماذج ورسائل الخطأ، بنية العناوين، قارئ شاشة واحد على الأقل أو دليل AT موثق، تكبير 200%، عرض 320px، وتقليل الحركة. اربط كل عائق بمسار/حالة/دور ونتيجة مهمة، وأصلح عوائق P0/P1 مع اختبار انحدار مناسب. معايير القبول: مصفوفة فعلية للصفحات والأدوار والتقنيات مع PASS/FAIL/NOT RUN ولقطات أو مخرجات قابلة للتتبع، ولا تدّع امتثال WCAG شامل من axe أو فحص آلي وحده. استخدم بيانات اختبارية أو جلسة قراءة فقط في النشر الحي؛ لا تغيّر سجلات الإنتاج.

## P13 — البيانات الرئيسية وجودتها والأداء الممثل

> افحص `scripts/data/master-data-catalog.ts` و`run-master-data-import.ts` و`Documents/MASTER-DATA-STARTING-DATA.md` و`Documents/DATA-MODEL.md` و`scripts/performance/seed-synthetic-dataset.ts` و`tests/performance/load-profiles.mjs`. ميّز مجموعات master data المعتمدة من الفئات غير المؤكدة؛ لا تحمل site/department/item/supplier/method ببيانات مخترعة. أضف مصدر ومالك ونسخة واعتمادًا لكل مجموعة قابلة للاستيراد، وفحوص تكرار وعلاقات وإعادة تشغيل idempotent مع سجل reconciliation. ابنِ dataset تركيبيًا يمثل كثافة الجداول والعلاقات والملفات والطوابير، وقِس P95/الذاكرة/DB pool مع وصف ظروف التشغيل؛ قرارات SLO والقدرة تبقى مفتوحة إن لم تعتمد. معايير القبول: import معتمد فقط، رفض المجموعة غير المعتمدة قبل أي كتابة، rollback على الفشل، تقارير lineage/quality قابلة لإعادة الإنتاج، وقياس أداء قبل/بعد لنفس السيناريو؛ لا seed للإنتاج.

## P14 — حوكمة AI وتقييمه مع الإنسان في الحلقة

> افحص `src/modules/ai-advisory/` و`src/pages/ai-advisory.astro` و`tests/integration/ai-advisory/evals.test.ts` و`security.test.ts` و`Documents/AI-PROVIDERS.md`. حافظ على الاستشارات كاقتراحات غير ملزمة، وعلى `AI_EXTERNAL_PROCESSING_APPROVED=false` افتراضيًا. وسّع مجموعة تقييم offline حتمية تغطي رفض ادعاء سلطة اعتماد، سياق مصدر ناقص، حقن prompt، أسرار/PII، استشهادات خاطئة، حالات عدم اليقين والتحويل لمراجع بشري؛ سجل نسخة dataset والنتائج ومعدل الخطأ لكل فئة على نفس SHA. صمم واجهة تعرض المصدر وحدود الاستشارة وسبب الامتناع ومسار مراجعة بشرية دون زر يوحي باعتماد QC آلي. معايير القبول: فشل مغلق قبل provider عند البيانات المحظورة، عدم كتابة قرار business من جواب AI، نتائج eval قابلة للإعادة، وخطة مراقبة drift/قرار تفعيل المزود موثقة. لا ترسل بيانات حقيقية لمزود خارجي ولا تفعّل المعالجة الخارجية دون اعتماد صاحب الصلاحية.

## P15 — مصالحة نهائية للمخاطر والمتطلبات وبوابات الإصدار

> بعد إنجاز البرومبتات المنطبقة، راجع `Documents/REQUIREMENTS-TRACEABILITY.md` و`REQUIREMENTS-RECONCILIATION.md` و`GAP-RISK-PRIORITY-MATRIX.md` و`RISK-REGISTER.md` و`src/modules/release-governance/` و`scripts/release/check-verification-evidence.mjs`. اربط كل فجوة مغلقة بكود/اختبار/دليل تشغيل أو قرار مالك على **نفس SHA**؛ لا تنقل نسب أو نجاحات تقارير قديمة. ولّد ملخص بوابات يميّز `PASS/FAIL/NOT RUN/BLOCKED/HISTORICAL`، ويحدد مالك كل blocker وفعل الإغلاق. تحقق من أن أدلة CI/security/database/E2E/UAT تدخل عبر مسار موثوق، وأن توقيع UAT البشري ونطاقه قراران صريحان لا نتيجة اختبار آلي. معايير القبول: اختلاف SHA أو نقص دليل يسبب NO-GO، والدليل المكتمل فقط يرفع بوابته، وأي نسبة جديدة تشرح المقام ووزن المجالات وطريقة الحساب وتاريخ الدليل. سلّم قرار جاهزية مشروطًا بالأدلة؛ لا deploy أو promotion أو إعلان `RELEASED` ضمن هذه المهمة.

## قرارات يجب طلبها من المالك عند نقطة الحاجة

1. **QC/QMS:** مصدر نتيجة التفتيش ومعايير PD-01/PD-02/PD-07، وحدود اعتماد المرحلتين.
2. **Security/QMS:** سياسة file allowlist/MIME والفحص والاحتفاظ ومسؤولية البيانات الحساسة في audit/export.
3. **Operations:** RPO/RTO/SLO والعتبات وجهة التنبيه وصلاحيات الاستعادة، وخطة migration والنشر بعد جاهزية الأدلة.
4. **Product/QMS:** نطاق موقّع UAT، متطلبات قبول المستخدمين، وسياسة معالجة AI الخارجية إن أُريد تفعيلها.

حتى صدور القرار، البرومبت التنفيذي يكمل الجزء القابل للتحقق محليًا ويحافظ على `DENY/NOT AVAILABLE/NOT VERIFIED` في المسار المعتمد.

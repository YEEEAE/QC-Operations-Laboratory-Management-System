# مراجعة تعليمات الوكلاء والمهارات والإضافات وMCP — 2026-09-23

المراجعة قراءة فقط. التوصيات لا تعني إثبات عدم الاستخدام؛ لا توجد بيانات تشغيل موثوقة لكل المهارات أو الإضافات. لم يُحذف أو يُغيّر أي إعداد.

| العنصر | الدليل المختصر | التوصية |
|---|---|---|
| `AGENTS.md` و`.agents/AGENTS.md` و`.claude/AGENTS.md` | 678/667/667 سطرًا؛ نسختا `.agents` و`.claude` متطابقتان حرفيًا، والجذر يزيد بقسم `Map` فقط. داخل النص تكرار لقواعد اكتشاف المهارات وMCP والصلاحيات. | **دمج**: مصدر واحد للقواعد، وملفات منصة قصيرة تشير إليه. احتفظ بقواعد منع commit/push والذاكرة دون تخفيف. |
| قاعدة قراءة كل مهارات المشروع قبل كل مهمة | 377 ملف `SKILL.md` بحجم 2.1 MB و42,327 سطرًا؛ تكلفة القراءة كبيرة حتى للمهمة الصغيرة. | **دمج**: فهرس وصفي آلي لجميع المهارات، ثم قراءة نص المهارات المرتبطة بالمهمة كاملًا. |
| `seo-geo/SKILL.md` | أربع نسخ ملفات عادية متطابقة حرفيًا في `.agents/skills/seo-geo/` وداخل `.agents/skills/.zcode/` و`.claude/` و`.agents/`. | **دمج**: نسخة أصلية واحدة وروابط أو إحالات واضحة بعد التحقق من متطلبات كل عميل. |
| `.agents/skills/ui-ux-pro-max` و`.opencode/skills/ui-ux-pro-max` | نسختان متقاربتان بالحجم، 28,032 و28,066 بايت؛ ليستا متطابقتين حرفيًا. | **دمج** بعد فحص فرق المحتوى ومتطلبات OpenCode؛ تجنّب تعديل نسخة واحدة وترك الثانية. |
| المهارات المطوّلة | 7 مهارات تتجاوز 500 سطر، أبرزها `distribb` 1,830، `design-taste-frontend` 1,206، `brandkit` 798؛ 42 تتجاوز 200 سطر. | **أرشفة** التفاصيل المرجعية الطويلة داخل ملفات `references/` والإبقاء على `SKILL.md` مختصرًا للمسار التشغيلي. |
| مهارات التسويق وSEO العامة | 150 مهارة أقصر من 60 سطرًا، وتوجد عائلات متداخلة في SEO والتصميم؛ لم يثبت أنها غير مستخدمة. | **إبقاء** حاليًا؛ راجع سجلات الاستدعاء قبل دمج أو أرشفة أي مهارة بعينها. |
| `.agents/plugins/installed_plugins.json` | يسجل 10 إضافات لـClaude، ثلاث منها إصدارها `unknown`؛ سجل تثبيت لا يثبت أنها نشطة في Codex. | **إبقاء** كسجل محلي، مع تحديث مصدر الإصدارات والتحقق من الاستخدام قبل أرشفة أي إضافة. |
| `.agents/plugins/marketplace.json` | يسجل إضافة واحدة و22 مهارة؛ هذه قائمة عرض وليست قائمة استخدام. | **إبقاء**، وتوضيح الفرق بينها وبين سجل التثبيت. |
| `.codex/config.toml` | يعرّف `caveman-shrink` و`dataforseo` و`postgres`؛ لم تظهر أدوات هذه الأسماء ضمن أدوات الجلسة الحالية. | **إبقاء** مؤقتًا؛ افحص تشغيل كل خادم واتصاله وسبب عدم ظهوره، ثم **أرشفة** إعداد أي خادم يتأكد توقف استخدامه. |
| `.opencode/opencode.json` | إضافة `opencode-chromium` واحدة؛ لا يثبت وجود الإعداد استخدامها في هذه الجلسة. | **إبقاء** إلى حين التحقق من استخدام OpenCode؛ **أرشفة** الإعداد إذا توقف استخدامه. |

**ملاحظة تقادم:** تواريخ 2020–2024 داخل بعض المهارات كانت في أمثلة أو سياق تاريخي، وليست وحدها دليلًا على معلومة قديمة. قواعد SEO التي تتغير مع المنصات تحتاج تحققًا من المصدر الرسمي قبل تحديثها.

**حدود الفحص:** شمل ملفات الإعداد المحلية والمهارات وبيانات الأدوات المكشوفة للجلسة. لم تُفحص سجلات استخدام فعلية لجميع العملاء، ولم يُختبر بدء خوادم MCP أو اتصالاتها، ولم تُراجع صحة كل حقيقة تقنية داخل 377 مهارة.

# Fixed 15-item finding-closure ledger (Prompt 11)

> Fail-closed ledger. Old PASS evidence from other SHAs/builds was NOT carried forward.
> Missing, stale, mismatched, or source-only evidence is recorded as OPEN.

## Ledger binding (single release candidate)

| Field | Value |
| --- | --- |
| Release-candidate ID | `rel-256b4f57b9618cc0` |
| Exact Git SHA | `94bf2763c94eda842eee390159ce2069a84ae2d9` |
| Build ID | `local-94bf2763c94e-ledger` |
| Environment | `local` |
| Build/deployment timestamp (ledger binding) | `2026-09-10T03:37:56.560Z` |
| Migration head | `0021_release_governance` (`f4f0525905e58d8d524e65d10b14eed425b3a2a5fe60434b88d12b9bb4ef139b`) |
| Working tree at binding | `clean`, `dirty: false` |
| Evidence file | `dist/release-identity-ledger.json` (generated by `pnpm release:identity --environment local --build-id local-94bf2763c94e-ledger`) |
| Deployment status | **NOT DEPLOYED** — local-only build. No staging/production deployment of this SHA exists. There is therefore no same-build live surface to score against. |

## Fresh automated verification executed for this SHA/build (2026-09-10, Asia/Riyadh)

| Command | Result |
| --- | --- |
| `pnpm exec vitest run tests/unit/health/canonical-readiness-agreement.test.ts tests/unit/admin tests/unit/ui/reflow-320.test.ts tests/unit/ui/mutation-post.test.ts tests/unit/change-requests/document-version-change-fields.test.ts tests/unit/ui/entity-select.test.ts tests/unit/ui/mobile-drawer-inert.test.ts tests/unit/ui/icon-and-copy-contract.test.ts tests/unit/ui/action-vocabulary.test.ts tests/unit/backup-recovery tests/unit/quarantine` | **19 files / 138 tests passed** |
| `pnpm exec vitest run tests/integration/health/health-agreement.test.ts tests/integration/shared/audit-dashboard-parity.test.ts tests/integration/change-requests/document-version-change-request.test.ts tests/integration/quarantine/template-lifecycle.test.ts` | **4 files / 28 tests passed** |
| `pnpm typecheck` | **0 errors** (62 hints, pre-existing) |
| `pnpm test:architecture` | **passed** (no Delivery violations) |
| `pnpm build` | **passed** (`Server built`, chunk-size warnings only) |
| Node runtime note | Local `v22.22.3` is outside contract `>=24.20.0 <25`; results are local-only |

## Live-evidence position for this build

- No Chrome run was executed against `rel-256b4f57b9618cc0` in this task.
- Production (`qclevel.top`) runs a previously published build, not this SHA. `/api/health/ready` exposes only `{"status":"healthy"}` with no SHA/build/RC identity surface to compare.
- Prior C-01..C-07 PASS and F-01/F-02/F-07 live confirmations belong to older SHAs/builds (e.g. `2adb787…`, `507d670`) and are therefore **stale/mismatched** for this ledger — not reused per Prompt 11.
- Authenticated E2E suites covering lifecycle/mutations/drawer/reflow remain **gated/skipped** without disposable fixtures + `QC_TEST_DATABASE_URL`/Docker and without a deployed candidate; Playwright `template-concurrency` real-PG test is likewise gated.
- Conclusion: every item below lacks same-build live evidence, so every item is **OPEN** even where same-SHA automated source tests pass.

## Item ledger (15/15)

| ID | Acceptance criterion re-run | Automated test (same SHA) / result | Live evidence reference (same build) | Reviewer / timestamp | Status | Limitation |
| --- | --- | --- | --- | --- | --- | --- |
| F-01 | Canonical DB readiness: both `/api/health/ready` probe and `/system/health` probe consume `checkCanonicalDatabaseReadiness` with identical TLS handling; three states agree; output sanitized | `tests/unit/health/canonical-readiness-agreement.test.ts` + `tests/integration/health/health-agreement.test.ts` inside batches above — **passed** | **Missing** — no same-build deployed health surface checked; prior `HEALTHY` view belongs to older build | agent verification / 2026-09-10 | **OPEN** | Source-only on this build; live agreement on deployed candidate not run |
| F-02 | Admin workspace: 8 `/admin/*` routes server-rendered, `canAccessAdminRoute` deny-by-default, explicit grant only, `passwordHash` never returned, mutations audited + session invalidation + self-block SoD | `tests/unit/admin/*` inside 138-test batch — **passed** | **Missing** — no same-build granted/denied panel check with real accounts; prior owner visibility was older build | agent verification / 2026-09-10 | **OPEN** | Source-only; negative-role runtime proof absent for this build |
| F-03 | 320px reflow + 200% zoom LTR/RTL: no page-level 2-D scroll, media/fields contained, tables scroll internally, drawer RTL-correct | `tests/unit/ui/reflow-320.test.ts` inside batch — **passed**; `tests/e2e/reflow-320.spec.ts` gated (18 auth tests skip without fixture) | **Missing** — no same-build Chrome 320px/200% run | agent verification / 2026-09-10 | **OPEN** | Static guards green; authenticated browser proof gated |
| F-04 | Nine create forms POST baseline: real POST + `Astro.callAction`, shared `mutation-post` helper, visible error summary + inline errors + focus management + retained values + conflict/dependency recovery, 303 redirect by id only | `tests/unit/ui/mutation-post.test.ts` inside batch — **passed** | **Missing** — no same-build no-JS/JS submit run against deployed candidate; prior summaries belong to older source | agent verification / 2026-09-10 | **OPEN** | Source-only; `create-forms-resilience` E2E gated |
| F-05 | Create-form resilience fresh run: POST baseline + Action enhancement, safe errors, retained values, stale/unauthorized rejection | Same `mutation-post` + POST-baseline coverage as F-04 — **passed (source)** | **Missing** — explicit fresh resilience run on this build not executed; old "POST baseline exists" is stale | agent verification / 2026-09-10 | **OPEN** | No fresh same-build execution evidence beyond unit contracts |
| F-06 | Contextual change request: authorized document/version selector; `targetId/version/snapshot/currentValue/dataType` server-derived in-transaction with version lock; allowlist exactly `revision`, `changeSummary`, `contentHash`; all else rejected | `tests/unit/change-requests/document-version-change-fields.test.ts` + `tests/integration/change-requests/document-version-change-request.test.ts` — **passed** | **Missing** — no same-build Chrome proof that operator UI hides UUID/JSON/field-path/data-type (prior C-06 PASS was older SHA) | agent verification / 2026-09-10 | **OPEN** | Source-only; live C-06 must be re-run on deployed candidate |
| F-07 | Audit parity: dashboard activity and `/audit` share mapper/ordering (`occurred_at DESC, event_no DESC`)/pagination/filters, `total` = full match count, never select `payload`, same qualifying event visible on both for same authorized actor | `tests/integration/shared/audit-dashboard-parity.test.ts` (9 tests) — **passed** | **Missing** — no same-build authenticated GRANT-event parity check in browser | agent verification / 2026-09-10 | **OPEN** | Source-only; negative-scope runtime proof absent |
| F-09 | Safe navigation context: Cancel/back on all create forms, `safeListHref` blocks open-redirect, equipment/template selectors authorized, business numbers remain manual (no invented numbering) | `tests/unit/ui/entity-select.test.ts` (14 tests) inside batch — **passed** | **Missing** — no same-build browser back/safe-return run | agent verification / 2026-09-10 | **OPEN** | Source-only |
| F-10 | P-06 template lifecycle: Employee→DRAFT only; Supervisor/Manager/yazeed create APPROVED via reauth+signature; review/approve (template-only SoD exception); stop/void/supersede with reason+reauth+signature; revise-new instead of edit; expected-version/stale rejection; atomic audit/outbox; idempotent requestId | `tests/unit/quarantine/*` + `tests/integration/quarantine/template-lifecycle.test.ts` (11 P-06 tests) — **passed**; `template-concurrency` real-PG **gated/skipped** (no `QC_TEST_DATABASE_URL`/Docker) | **Missing** — no same-build authenticated lifecycle pass of every action on deployed candidate | agent verification / 2026-09-10 | **OPEN** | Partial: concurrency/E2E gated; live lifecycle absent |
| F-11 | Operational evidence without false-green: R2 artifact store via S3 API (not described as PG/PITR), daily backup job fail-closed, 30-day retention, SHA-256 catalog + read-after-write, monthly isolated restore drill blocked without explicit target, yazeed-only production restore with reauth+signature+exact artifact identity, RPO=24h/RTO=4h targets displayed separately from measured values | `tests/unit/backup-recovery/*` (adapters + backup-job) inside batch — **passed** | **Missing** — no verified artifact and no successful isolated restore exist for this release; prior `RER-2026-09-08-001` drill is stale (different HEAD) and not reused | agent verification / 2026-09-10 | **OPEN** | Catalog wiring + live artifact + isolated drill all absent for this build |
| F-12 | English-only LTR UI + sentence-case action vocabulary; no Arabic/RTL branch; controlled transition terms preserved; scientific terms untouched | `tests/unit/ui/action-vocabulary.test.ts` inside batch — **passed** | **Missing** — no same-build full-surface Chrome copy sweep (prior EN-only views were older build) | agent verification / 2026-09-10 | **OPEN** | Source-only; `UI-UX-SPECIFICATION.md` Arabic/RTL text conflict still documented and unresolved |
| BI-01 | Mobile drawer a11y: open drawer makes `.app-workspace` `inert`, locks body scroll, contains Tab/Shift+Tab, Escape closes, focus returns to toggle, breakpoint exit clears all temp state, respects reduced-motion | `tests/unit/ui/mobile-drawer-inert.test.ts` (8 guards) inside batch — **passed**; shipped-chunk harness was a prior SHA, not re-run here; `tests/e2e/mobile-drawer-inert.spec.ts` (5 scenarios) **gated** | **Missing** — C-09 containment/escape never ran on this build (at 320px the toggle was hidden on the old build) | agent verification / 2026-09-10 | **OPEN** | Source-only; live keyboard containment absent |
| BI-02 | Single local SVG icon system: one registry, `24×24`/`currentColor`/uniform stroke, `aria-hidden` decorative, zero Unicode glyphs in navigation/shell | `tests/unit/ui/icon-and-copy-contract.test.ts` inside batch — **passed** | **Missing** — no same-build Chrome SVG audit (`svgCount`/`badIcons`) on this build | agent verification / 2026-09-10 | **OPEN** | Source-only |
| BI-03 | Operator writing/flow (same root cause as F-06): no `Target version identifier` / `Target snapshot (JSON)` / `Field path` / `Data type` exposed to operator; human labels + structured editor only | Covered by same F-06 allowlist + `entity-select` contract tests — **passed (source)**; evidence deduplicated with F-06 but evaluated independently | **Missing** — same as F-06: no same-build Chrome proof | agent verification / 2026-09-10 | **OPEN** | Source-only; shares F-06 live gap |
| BI-04 | User-facing copy: no `authorized read model` / internal architecture terms; direct operational wording preserving authorization meaning | `icon-and-copy-contract` banned-terms guard inside batch — **passed** | **Missing** — no same-build full copy sweep in browser | agent verification / 2026-09-10 | **OPEN** | Source-only |

## Metric (strict, fixed denominators — no averaging, no rounding up)

| Metric | Computation | Result |
| --- | --- | --- |
| Finding closure | `CLOSED / 15` | **0 / 15 = 0.0%** (15 OPEN, 0 CLOSED) |
| Unresolved HIGH / MEDIUM | F-06 MEDIUM, F-10 MEDIUM, F-11 MEDIUM, BI-01 MEDIUM, BI-02 MEDIUM, BI-03 MEDIUM remain | **6 MEDIUM unresolved** (no new HIGH confirmed) |
| 100.0% gate | Requires 15 CLOSED of 15 for the same deployed build + zero unresolved HIGH/MEDIUM | **NOT MET** — 100.0% is forbidden on this ledger |

## What would close items on a future ledger

1. Deploy this exact SHA + build identity to a verifiable environment and expose sanitized SHA/build/RC/environment/timestamp (Prompt 12).
2. Provision disposable role fixtures (Prompt 13) and run C-01..C-12 on the unchanged identity (Prompts 14–15), attaching timestamped screenshots/snapshots.
3. Produce a verified backup artifact + successful isolated restore drill bound to that same release (F-11), and run the full template lifecycle authenticated on that candidate (F-10).
4. Create a NEW fixed ledger bound to that deployed identity; only entries with same-build automated + live evidence become CLOSED.

## Files touched by this task

- `audit/15-item-closure-ledger-rel-256b4f57b9618cc0.md` (new — this ledger)
- `dist/release-identity-ledger.json` (new — ignored build evidence, not committed source)
- `.agents/mind/01-mind-latest.md` (mind record appended)

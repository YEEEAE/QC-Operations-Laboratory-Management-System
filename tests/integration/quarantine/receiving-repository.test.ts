import { describe, expect, it } from 'vitest'; import { createReceivingItem } from '../../../src/modules/quarantine/receiving/domain/receiving-item.js'; describe('receiving repository contract inputs',()=>{it('requires positive quantity and initializes release false',()=>{const x=createReceivingItem({id:'01900000-0000-7000-8000-000000000001',receivingNo:'RCV-1',docNo:'D',itemCode:'I',description:'M',lot:'L',qty:1,receivingDate:new Date(),createdBy:'u',now:new Date()});expect(x.qty).toBe('1');expect(x.releaseSystem).toBe(false);});});

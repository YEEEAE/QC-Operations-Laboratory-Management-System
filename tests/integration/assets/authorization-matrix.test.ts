import { describe, expect, it } from 'vitest';
import { authorize } from '../../../src/shared/authorization/authorize.js';
describe('Assets authorization matrix', () => { it('denies missing asset permission by default', () => { const actor = { id: 'u', accountState: 'ACTIVE' as const, roles: ['Admin'], permissions: [] }; expect(() => authorize({ actor, permission: 'PERM-EQP-VIEW', action: 'VIEW', entity: { type: 'EQUIPMENT', id: 'e', state: 'ACTIVE', ownerId: 'u' }, scope: { ownerId: 'u' }, currentVersion: 1n, expectedVersion: 1n, businessCondition: true }, { throwOnDeny: true })).toThrow(); }); it('denies stale versions even with explicit permission', () => { const actor = { id: 'u', accountState: 'ACTIVE' as const, roles: ['Supervisor'], permissions: [{ code: 'PERM-EQP-EDIT' as const, scopes: ['OWN' as const] }] }; expect(() => authorize({ actor, permission: 'PERM-EQP-EDIT', action: 'EDIT', entity: { type: 'EQUIPMENT', id: 'e', state: 'DRAFT', ownerId: 'u' }, scope: { ownerId: 'u' }, currentVersion: 2n, expectedVersion: 1n, businessCondition: true }, { throwOnDeny: true })).toThrow(); }); });

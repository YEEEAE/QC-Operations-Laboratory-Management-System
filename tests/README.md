# Tests

Tests provide evidence for behavior; a passing build is not evidence of authorization, state-machine, concurrency, or recovery correctness.

Use focused unit tests by default. Add integration, negative authorization, concurrency/idempotency, migration, and E2E coverage as each domain implementation requires it.

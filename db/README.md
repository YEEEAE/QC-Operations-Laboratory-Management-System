# Database

This folder is reserved for PostgreSQL migrations and database-owned operational artifacts. Migrations are immutable once applied; they must not be imported by the Delivery layer.

Database provider, backup topology, and retention remain deferred where the approved Foundation documents say so.

---
import AppLayout from '../../../../../ui/layouts/AppLayout.astro';const actor=Astro.locals.actor;if(!actor)return Astro.redirect(`/login?returnTo=${encodeURIComponent(Astro.url.pathname)}`);
---
<AppLayout title="Retest request"><h1>Retest</h1><p>Retest is blocked until the approved policy supplies authority, count, sequence, and original-result handling. The original test cannot be overwritten.</p></AppLayout>

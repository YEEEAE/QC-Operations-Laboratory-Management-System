import { defineAction, ActionError } from 'astro:actions';
import { z } from 'astro:schema';
import { assetsActionDependencies } from '../modules/assets/application/dependencies.js';
import { toActionError } from '../shared/errors/action-error.js';
import { AppError } from '../shared/errors/app-error.js';
type ActionContext = { locals: App.Locals };
const actor = (context: ActionContext) => { if (!context.locals.actor) throw new AppError('AUTH_REQUIRED', { userSafe: true }); return context.locals.actor; };
const requestId = (context: ActionContext) => context.locals.requestContext?.requestId ?? 'unknown';
const run = async <T>(work: () => Promise<T>, context: ActionContext): Promise<T> => { try { return await work(); } catch (error) { const mapped = toActionError(error, requestId(context)); throw new ActionError({ code: 'BAD_REQUEST', message: mapped.error.messageKey }); } };
const id = z.string().uuid(); const version = z.coerce.bigint(); const optionalDate = z.coerce.date().optional();
const createEquipment = defineAction({ accept: 'json', input: z.object({ equipmentNo: z.string(), name: z.string(), manufacturer: z.string().optional(), model: z.string().optional(), serialNo: z.string().optional(), location: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().equipment.create.execute({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
const updateEquipment = defineAction({ accept: 'json', input: z.object({ equipmentId: id, expectedVersion: version, equipmentNo: z.string(), name: z.string(), manufacturer: z.string().optional(), model: z.string().optional(), serialNo: z.string().optional(), location: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().equipment.update.execute({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
const transitionEquipment = defineAction({ accept: 'json', input: z.object({ equipmentId: id, expectedVersion: version, action: z.enum(['ACTIVATE','OUT_OF_SERVICE','START_MAINTENANCE','RETURN_TO_SERVICE','DECOMMISSION']), reason: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().equipment.update.transition({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
const createCalibration = defineAction({ accept: 'json', input: z.object({ calibrationNo: z.string(), equipmentId: id, calibrationDate: z.coerce.date(), dueDate: optionalDate, provider: z.string().optional(), certificateNo: z.string().optional(), result: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().calibration.create.execute({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
const transitionCalibration = defineAction({ accept: 'json', input: z.object({ calibrationId: id, expectedVersion: version, action: z.enum(['SUBMIT','APPROVE','MAKE_CURRENT','MARK_DUE','MARK_OVERDUE','SUPERSEDE','VOID']), reason: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().calibration.transition.execute({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
const createMaintenance = defineAction({ accept: 'json', input: z.object({ maintenanceNo: z.string(), equipmentId: id, maintenanceType: z.string().optional(), description: z.string(), plannedAt: optionalDate, performedBy: z.string().optional(), provider: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().maintenance.create.execute({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
const transitionMaintenance = defineAction({ accept: 'json', input: z.object({ maintenanceId: id, expectedVersion: version, action: z.enum(['PLAN','START','COMPLETE','CANCEL','VOID']), reason: z.string().optional() }), handler: (input, context) => run(() => assetsActionDependencies().maintenance.transition.execute({ ...input, actor: actor(context), requestId: requestId(context) }), context) });
export const assets = { createEquipment, updateEquipment, transitionEquipment, createCalibration, transitionCalibration, createMaintenance, transitionMaintenance };

import { AppError } from '../../../shared/errors/app-error.js';
import { uuidv7 } from '../../../shared/id/uuid.js';
import type { ActorContext } from '../../../shared/authorization/types.js';
import { validateMeasurement, type MeasurementInput } from '../domain/measurement.js';
import type { LabSample } from '../domain/lab-test.js';
import { authorizeLab } from './lab-authorization.js';
import type { LabRepository } from '../ports/repository.js';
export class SaveMeasurementsUseCase { constructor(private readonly repository:LabRepository,private readonly now=()=>new Date()) {} async execute(input:{actor:ActorContext;id:string;expectedVersion:bigint;samples:readonly LabSample[];measurements:readonly MeasurementInput[];requestId:string}) { const test=await this.repository.get(input.id,input.actor); if(!test)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true}); authorizeLab(input.actor,test,'PERM-LAB-EDIT-DRAFT','SAVE',input.expectedVersion); authorizeLab(input.actor,test,'PERM-LAB-ENTER-MEASUREMENT','SAVE',input.expectedVersion); if(!input.samples.length||input.samples.some(s=>!s.id||!s.identifier.trim())||new Set(input.samples.map(s=>s.id)).size!==input.samples.length||input.measurements.some(m=>!input.samples.some(s=>s.id===m.sampleId)))throw new AppError('VALIDATION_FAILED',{userSafe:true}); const at=this.now().toISOString(); const measurements=input.measurements.map(m=>({...validateMeasurement(m,test.context.parameters.find(p=>p.id===m.parameterId) ?? (()=>{throw new AppError('VALIDATION_FAILED')})()),id:uuidv7(),enteredBy:input.actor.id,enteredAt:at})); const next={...test,samples:input.samples,measurements,version:test.version+1n,updatedAt:at}; return this.repository.save(test,next,{actor:input.actor,requestId:input.requestId,action:'SAVE'}); } }

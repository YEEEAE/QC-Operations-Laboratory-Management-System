import { AppError } from '../../../shared/errors/app-error.js'; import type { ActorContext } from '../../../shared/authorization/types.js'; import { transitionLab } from '../domain/lab-state.js'; import type { ControlledLabSources,LabApprovalPolicy } from '../ports/controlled-sources.js'; import type { LabRepository } from '../ports/repository.js'; import { authorizeLab } from './lab-authorization.js';
const denyPolicy:LabApprovalPolicy={authorize:async()=>{throw new AppError('AUTHZ_DENIED',{userSafe:true});}};
export class ApproveLabTestUseCase { constructor(private readonly repository:LabRepository,private readonly sources:ControlledLabSources,private readonly policy:LabApprovalPolicy=denyPolicy,private readonly now=()=>new Date()) {} async execute(input:{actor:ActorContext;id:string;expectedVersion:bigint;requestId:string}) {const test=await this.repository.get(input.id,input.actor);if(!test)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});authorizeLab(input.actor,test,'PERM-LAB-APPROVE','APPROVE',input.expectedVersion,true);authorizeLab(input.actor,test,'PERM-APR-APPROVE','APPROVE',input.expectedVersion,true);await this.policy.authorize({test,actor:input.actor,expectedVersion:input.expectedVersion});const evaluation=await this.sources.evaluate(test);if(evaluation.sourceReference!==test.context.sourceReference||evaluation.contentHash!==test.context.contentHash)throw new AppError('AUTHZ_DENIED',{userSafe:true});const at=this.now().toISOString();return this.repository.save(test,{...test,state:transitionLab(test.state,'APPROVE'),scientificResult:evaluation.result,version:test.version+1n,updatedAt:at,approvedAt:at},{actor:input.actor,requestId:input.requestId,action:'APPROVE'});}}

import { AppError } from '../../../shared/errors/app-error.js'; import type { ActorContext } from '../../../shared/authorization/types.js'; import { transitionLab } from '../domain/lab-state.js'; import type { LabRepository } from '../ports/repository.js'; import { authorizeLab } from './lab-authorization.js';
export class ReturnLabTestUseCase { constructor(private readonly repository:LabRepository,private readonly now=()=>new Date()) {} async execute(input:{actor:ActorContext;id:string;expectedVersion:bigint;reason:string;requestId:string}) {const test=await this.repository.get(input.id,input.actor);if(!test)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});if(!input.reason.trim())throw new AppError('VALIDATION_FAILED',{userSafe:true});authorizeLab(input.actor,test,'PERM-LAB-RETURN','RETURN',input.expectedVersion,true);const at=this.now().toISOString();return this.repository.save(test,{...test,state:transitionLab(test.state,'RETURN'),version:test.version+1n,updatedAt:at},{actor:input.actor,requestId:input.requestId,action:'RETURN',reason:input.reason.trim()});}}

import { AppError } from '../../../shared/errors/app-error.js'; import { uuidv7 } from '../../../shared/id/uuid.js'; import type { ActorContext } from '../../../shared/authorization/types.js'; import { assertRetestLink } from '../domain/retest.js'; import type { ControlledLabSources,RetestPolicy } from '../ports/controlled-sources.js'; import type { LabRepository } from '../ports/repository.js';
const denyPolicy:RetestPolicy={authorize:async()=>{throw new AppError('AUTHZ_DENIED',{userSafe:true});}};
export class CreateRetestUseCase { constructor(private readonly repository:LabRepository,private readonly sources:ControlledLabSources,private readonly policy:RetestPolicy=denyPolicy,private readonly now=()=>new Date()) {} async execute(input:{actor:ActorContext;originalId:string;reason:string;requestId:string}) {const original=await this.repository.get(input.originalId,input.actor);if(!original)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});if(!input.reason.trim())throw new AppError('VALIDATION_FAILED',{userSafe:true});const authorization=await this.policy.authorize({original,actor:input.actor,reason:input.reason.trim()});const context=await this.sources.resolve(authorization.templateVersionId,input.actor);const at=this.now().toISOString();const next={...original,id:uuidv7(),labTestNo:authorization.labTestNo,state:'DRAFT' as const,scientificResult:null,authorId:input.actor.id,createdBy:input.actor.id,version:1n,context,samples:[],measurements:[],originalTestId:original.id,retestSequence:authorization.sequence,retestReason:input.reason.trim(),createdAt:at,updatedAt:at,submittedAt:null,reviewStartedAt:null,approvedAt:null};assertRetestLink(original,next);return this.repository.create(next,{actor:input.actor,requestId:input.requestId,action:'CREATE_RETEST'});}}

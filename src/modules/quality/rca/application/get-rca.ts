import type{ActorContext}from'../../../../shared/authorization/types.js';import type{RcaRepository}from'../ports/repository.js';import{AppError}from'../../../../shared/errors/app-error.js';export class GetRcaUseCase{constructor(private repo:RcaRepository){}async execute(i:{actor:ActorContext;id:string}){const r=await this.repo.get(i.id,i.actor);if(!r)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});return r;}}

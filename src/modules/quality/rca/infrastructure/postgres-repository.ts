import type{Kysely}from'kysely';import type{DatabaseSchema}from'../../../../shared/database/db-types.js';import type{RcaRepository}from'../ports/repository.js';import type{ActorContext}from'../../../../shared/authorization/types.js';export class PostgresRcaRepository implements RcaRepository{constructor(private db:Kysely<DatabaseSchema>){}private map(r:any):any{return{id:r.id,rcaNo:r.rca_no??undefined,ncrId:r.ncr_id,state:r.state,method:r.method??undefined,analysis:r.analysis??undefined,rootCause:r.root_cause??undefined,submittedAt:r.submitted_at??undefined,approvedAt:r.approved_at??undefined,createdBy:r.created_by,createdAt:r.created_at,updatedAt:r.updated_at,version:BigInt(r.version)}}async get(id:string,a:ActorContext){if(!/^[0-9a-f-]{36}$/i.test(id))return;const r=await this.db.selectFrom('rcas').selectAll().where('id','=',id).where('created_by','=',a.id).executeTakeFirst();return r&&this.map(r)}async list(i:any){let q=this.db.selectFrom('rcas').selectAll().where('created_by','=',i.actor.id);if(i.ncrId)q=q.where('ncr_id','=',i.ncrId)as any;return(await q.execute()).map((r:any)=>this.map(r))}async update(i:any){const r=await this.db.updateTable('rcas').set({method:i.rca.method??null,analysis:i.rca.analysis??null,root_cause:i.rca.rootCause??null,version:i.rca.version,updated_at:i.rca.updatedAt}as any).where('id','=',i.rca.id).returningAll().executeTakeFirstOrThrow();return this.map(r)}async transition(i:any){const next:any={START:'IN_PROGRESS',SUBMIT:'SUBMITTED',RETURN:'RETURNED',APPROVE:'APPROVED',VOID:'VOID'};const r=await this.db.updateTable('rcas').set({state:next[i.action],updated_at:new Date(),submitted_at:i.action==='SUBMIT'?new Date():null,approved_at:i.action==='APPROVE'?new Date():null}as any).where('id','=',i.id).where('version','=',i.expectedVersion).returningAll().executeTakeFirst();if(!r)throw new Error('stale');return this.map(r)}}

import type{ActorContext}from'../../../../shared/authorization/types.js';import type{NcrRepository}from'../ports/repository.js';import{AppError}from'../../../../shared/errors/app-error.js';export class GetNcrUseCase{constructor(private repo:NcrRepository){}async execute(i:{actor:ActorContext;id:string}){const n=await this.repo.get(i.id,i.actor);if(!n)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});return n;}}

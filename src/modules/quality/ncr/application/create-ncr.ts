import {authorize} from '../../../../shared/authorization/authorize.js';import {uuidv7} from '../../../../shared/id/uuid.js';import type{ActorContext}from'../../../../shared/authorization/types.js';import{createNcr}from'../domain/ncr.js';import type{NcrRepository}from'../ports/repository.js';export class CreateNcrUseCase{constructor(private repo:NcrRepository,private now=()=>new Date()){}execute(i:{actor:ActorContext;ncrNo:string;title:string;description:string;findingId:string;affectedItemCode?:string;affectedLot?:string;ownerId?:string;requestId:string}){authorize({actor:i.actor,permission:'PERM-NCR-CREATE',action:'CREATE',entity:{type:'NCR',id:'new',state:'DRAFT',ownerId:i.ownerId??i.actor.id},scope:{ownerId:i.ownerId??i.actor.id},currentVersion:1n,expectedVersion:1n,businessCondition:Boolean(i.findingId)},{throwOnDeny:true});return this.repo.create({ncr:createNcr({id:uuidv7(),createdBy:i.actor.id,now:this.now(),...i}),actor:i.actor,requestId:i.requestId});}}

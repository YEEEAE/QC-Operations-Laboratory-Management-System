import type{ActorContext}from'../../../../shared/authorization/types.js';import type{CapaRepository}from'../ports/repository.js';import{AppError}from'../../../../shared/errors/app-error.js';export class GetCapaUseCase{constructor(private repo:CapaRepository){}async execute(i:{actor:ActorContext;id:string}){const c=await this.repo.get(i.id,i.actor);if(!c)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});return c;}}

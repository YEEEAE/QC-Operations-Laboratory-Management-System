import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { Capa,CapaActionType } from '../domain/capa.js'; export interface CapaRepository { create(i:{capa:Capa;actor:ActorContext;requestId:string}):Promise<Capa>; get(id:string,actor:ActorContext):Promise<Capa|undefined>; list(i:{actor:ActorContext;state?:Capa['state']}):Promise<readonly Capa[]>; transition(i:{id:string;expectedVersion:bigint;actor:ActorContext;action:CapaActionType;reason?:string;requestId:string;conditions?:{verified:boolean;effectivenessAccepted:boolean}}):Promise<Capa>; }

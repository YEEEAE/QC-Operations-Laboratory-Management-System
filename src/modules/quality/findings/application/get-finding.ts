import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { FindingRepository } from '../ports/repository.js'; import { AppError } from '../../../../shared/errors/app-error.js'; export class GetFindingUseCase {constructor(private repo:FindingRepository){} async execute(i:{actor:ActorContext;id:string}){const f=await this.repo.get(i.id,i.actor);if(!f)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});return f;}}

import { authorize } from '../../../../shared/authorization/authorize.js'; import { uuidv7 } from '../../../../shared/id/uuid.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import { createFinding } from '../domain/finding.js'; import type { FindingRepository } from '../ports/repository.js'; export class CreateFindingUseCase { constructor(private repo:FindingRepository,private now=()=>new Date()){} execute(i:{actor:ActorContext;findingNo:string;title:string;description:string;severity?:string;sourceContext?:Readonly<Record<string,unknown>>;ownerId?:string;requestId:string}){ authorize({actor:i.actor,permission:'PERM-FIND-CREATE',action:'CREATE',entity:{type:'FINDING',id:'new',state:'DRAFT',ownerId:i.ownerId??i.actor.id},scope:{ownerId:i.ownerId??i.actor.id},currentVersion:1n,expectedVersion:1n,businessCondition:true},{throwOnDeny:true}); return this.repo.create({finding:createFinding({id:uuidv7(),createdBy:i.actor.id,...i,now:this.now()}),actor:i.actor,requestId:i.requestId}); } }

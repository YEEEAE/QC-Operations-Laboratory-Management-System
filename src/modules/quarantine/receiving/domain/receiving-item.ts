import { AppError } from '../../../../shared/errors/app-error.js'; import { assertReason,transitionReceiving } from './receiving-state.js'; import type { InspectionResult,ReceivingAction,ReceivingWorkflowState } from './receiving-state.js';
export interface ReceivingItem { id:string; receivingNo:string; docNo:string; itemCode:string; description:string; lot:string; qty:string; receivingDate:Date; expiryDate?:Date; workflowState:ReceivingWorkflowState; inspectionResult:InspectionResult; releaseSystem:false; createdBy:string; createdAt:Date; updatedBy?:string; updatedAt:Date; version:bigint; }
export interface NewReceivingItem { id:string; receivingNo:string; docNo:string; itemCode:string; description:string; lot:string; qty:string|number; receivingDate:Date; expiryDate?:Date; createdBy:string; now:Date; }
const required=(v:string,n:string)=>{if(!v.trim())throw new AppError('VALIDATION_FAILED',{userSafe:true,fieldErrors:{[n]:['required']}});return v.trim()};
export function createReceivingItem(i:NewReceivingItem):ReceivingItem{const qty=String(i.qty);if(!(Number(qty)>0))throw new AppError('VALIDATION_FAILED',{userSafe:true});return {id:i.id,receivingNo:required(i.receivingNo,'receivingNo'),docNo:required(i.docNo,'docNo'),itemCode:required(i.itemCode,'itemCode'),description:required(i.description,'description'),lot:required(i.lot,'lot'),qty,receivingDate:i.receivingDate,expiryDate:i.expiryDate,workflowState:'PENDING',inspectionResult:'NOT_STARTED',releaseSystem:false,createdBy:i.createdBy,createdAt:i.now,updatedAt:i.now,version:1n};}
export function applyReceivingAction(item:ReceivingItem,action:ReceivingAction,reason?:string):ReceivingItem{if(['HOLD','CANCEL','MARK_EXPIRED'].includes(action))assertReason(reason);if(action==='REMOVE_HOLD')throw new AppError('AUTHZ_DENIED',{userSafe:true});return {...item,workflowState:transitionReceiving(item.workflowState,action)};}

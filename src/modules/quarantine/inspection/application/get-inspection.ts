import{authorize}from'../../../../shared/authorization/authorize.js';import{AppError}from'../../../../shared/errors/app-error.js';import type{ActorContext}from'../../../../shared/authorization/types.js';import type{InspectionRepository}from'../ports/repository.js';export class GetInspectionUseCase{constructor(private repo:InspectionRepository){}async execute(i:{actor:ActorContext;id:string}){const x=await this.repo.get(i.id,i.actor);if(!x)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});authorize({actor:i.actor,permission:'PERM-INSP-VIEW',action:'VIEW',entity:{type:'INSPECTION_REPORT',id:x.id,state:x.state,authorId:x.authorId,executorId:x.authorId},scope:{ownerId:x.authorId,assigneeId:x.authorId},currentVersion:x.version,expectedVersion:x.version,businessCondition:true},{throwOnDeny:true});return x;}}

import{authorize}from'../../../../shared/authorization/authorize.js';import{AppError}from'../../../../shared/errors/app-error.js';import{applyInspectionAction}from'../domain/inspection.js';import type{ActorContext}from'../../../../shared/authorization/types.js';import type{InspectionRepository}from'../ports/repository.js';export class SubmitInspectionUseCase{constructor(private repo:InspectionRepository){}async execute(i:{actor:ActorContext;id:string;expectedVersion:bigint;requestId:string}){const x=await this.repo.get(i.id,i.actor);if(!x)throw new AppError('RESOURCE_NOT_FOUND',{userSafe:true});applyInspectionAction(x,'SUBMIT');authorize({actor:i.actor,permission:'PERM-INSP-SUBMIT',action:'SUBMIT',entity:{type:'INSPECTION_REPORT',id:x.id,state:x.state,authorId:x.authorId,executorId:x.authorId},scope:{ownerId:x.authorId,assigneeId:x.authorId},currentVersion:x.version,expectedVersion:i.expectedVersion,businessCondition:true},{throwOnDeny:true});return this.repo.transition({id:i.id,expectedVersion:i.expectedVersion,actor:i.actor,action:'SUBMIT',requestId:i.requestId});}}

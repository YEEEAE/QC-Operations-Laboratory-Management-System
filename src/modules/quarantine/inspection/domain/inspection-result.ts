import { AppError } from '../../../../shared/errors/app-error.js'; export const FINAL_RESULTS=['PASS','FAIL','HOLD'] as const; export type FinalResult=typeof FINAL_RESULTS[number]; export interface InspectionResultEntry{ id:string; pointId:string; value:string|number|boolean; unit?:string; result?:string; remarks?:string; version:bigint; } export function assertResultSeparation(result:FinalResult|undefined){if(result&&!FINAL_RESULTS.includes(result))throw new AppError('VALIDATION_FAILED',{userSafe:true});}

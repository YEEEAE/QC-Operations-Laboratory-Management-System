import { authorize } from '../../../../shared/authorization/authorize.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import { uuidv7 } from '../../../../shared/id/uuid.js'; import { createDraftCalibration } from '../domain/calibration.js'; import type { CalibrationRepository } from '../ports/repository.js';
export class CreateCalibrationUseCase { constructor(private readonly repository: CalibrationRepository, private readonly now = () => new Date()) {} execute(input: { actor: ActorContext; calibrationNo: string; equipmentId: string; calibrationDate: Date; dueDate?: Date; provider?: string; certificateNo?: string; result?: string; requestId: string }) { authorize({ actor: input.actor, permission: 'PERM-CAL-CREATE', action: 'CREATE', entity: { type: 'CALIBRATION_RECORD', id: 'new', state: 'DRAFT', ownerId: input.actor.id }, scope: { ownerId: input.actor.id }, currentVersion: 1n, expectedVersion: 1n, businessCondition: Boolean(input.equipmentId) }, { throwOnDeny: true }); return this.repository.create({ calibration: createDraftCalibration({ ...input, id: uuidv7(), createdBy: input.actor.id, now: this.now() }), actor: input.actor, requestId: input.requestId }); } }

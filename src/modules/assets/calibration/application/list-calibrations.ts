import { authorize } from '../../../../shared/authorization/authorize.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { CalibrationListFilter, CalibrationRepository } from '../ports/repository.js';
export class ListCalibrationsUseCase { constructor(private readonly repository: CalibrationRepository) {} execute(input: { actor: ActorContext; filter?: CalibrationListFilter }) { authorize({ actor: input.actor, permission: 'PERM-CAL-VIEW', action: 'VIEW', entity: { type: 'CALIBRATION_RECORD', id: 'list', state: input.filter?.state ?? 'DRAFT', ownerId: input.actor.id }, scope: { ownerId: input.actor.id }, currentVersion: 1n, expectedVersion: 1n, businessCondition: true }, { throwOnDeny: true }); return this.repository.list(input); } }

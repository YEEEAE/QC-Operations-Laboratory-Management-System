import { authorize } from '../../../../shared/authorization/authorize.js'; import { AppError } from '../../../../shared/errors/app-error.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { CalibrationRepository } from '../ports/repository.js';
export class GetCalibrationUseCase { constructor(private readonly repository: CalibrationRepository) {} async execute(input: { actor: ActorContext; calibrationId: string }) { const calibration = await this.repository.get(input.calibrationId, input.actor); if (!calibration) throw new AppError('RESOURCE_NOT_FOUND', { userSafe: true }); authorize({ actor: input.actor, permission: 'PERM-CAL-VIEW', action: 'VIEW', entity: { type: 'CALIBRATION_RECORD', id: calibration.id, state: calibration.state, ownerId: calibration.createdBy }, scope: { ownerId: calibration.createdBy }, currentVersion: calibration.version, expectedVersion: calibration.version, businessCondition: true }, { throwOnDeny: true }); return calibration; } }

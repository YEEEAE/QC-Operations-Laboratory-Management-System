import { authorize } from '../../../../shared/authorization/authorize.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { MaintenanceListFilter, MaintenanceRepository } from '../ports/repository.js';
export class ListMaintenanceUseCase { constructor(private readonly repository: MaintenanceRepository) {} execute(input: { actor: ActorContext; filter?: MaintenanceListFilter }) { authorize({ actor: input.actor, permission: 'PERM-MNT-VIEW', action: 'VIEW', entity: { type: 'MAINTENANCE_RECORD', id: 'list', state: input.filter?.state ?? 'DRAFT', ownerId: input.actor.id }, scope: { ownerId: input.actor.id }, currentVersion: 1n, expectedVersion: 1n, businessCondition: true }, { throwOnDeny: true }); return this.repository.list(input); } }

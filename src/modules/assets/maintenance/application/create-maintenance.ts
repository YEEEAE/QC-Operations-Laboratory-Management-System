import { authorize } from '../../../../shared/authorization/authorize.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import { uuidv7 } from '../../../../shared/id/uuid.js'; import { createDraftMaintenance } from '../domain/maintenance.js'; import type { MaintenanceRepository } from '../ports/repository.js';
export class CreateMaintenanceUseCase { constructor(private readonly repository: MaintenanceRepository, private readonly now = () => new Date()) {} execute(input: { actor: ActorContext; maintenanceNo: string; equipmentId: string; maintenanceType?: string; description: string; plannedAt?: Date; performedBy?: string; provider?: string; requestId: string }) { authorize({ actor: input.actor, permission: 'PERM-MNT-CREATE', action: 'CREATE', entity: { type: 'MAINTENANCE_RECORD', id: 'new', state: 'DRAFT', ownerId: input.actor.id }, scope: { ownerId: input.actor.id }, currentVersion: 1n, expectedVersion: 1n, businessCondition: Boolean(input.equipmentId) }, { throwOnDeny: true }); return this.repository.create({ maintenance: createDraftMaintenance({ ...input, id: uuidv7(), createdBy: input.actor.id, now: this.now() }), actor: input.actor, requestId: input.requestId }); } }

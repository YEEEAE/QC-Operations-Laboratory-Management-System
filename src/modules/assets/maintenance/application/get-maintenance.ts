import { authorize } from '../../../../shared/authorization/authorize.js'; import { AppError } from '../../../../shared/errors/app-error.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { MaintenanceRepository } from '../ports/repository.js';
export class GetMaintenanceUseCase { constructor(private readonly repository: MaintenanceRepository) {} async execute(input: { actor: ActorContext; maintenanceId: string }) { const maintenance = await this.repository.get(input.maintenanceId, input.actor); if (!maintenance) throw new AppError('RESOURCE_NOT_FOUND', { userSafe: true }); authorize({ actor: input.actor, permission: 'PERM-MNT-VIEW', action: 'VIEW', entity: { type: 'MAINTENANCE_RECORD', id: maintenance.id, state: maintenance.state, ownerId: maintenance.createdBy }, scope: { ownerId: maintenance.createdBy }, currentVersion: maintenance.version, expectedVersion: maintenance.version, businessCondition: true }, { throwOnDeny: true }); return maintenance; } }

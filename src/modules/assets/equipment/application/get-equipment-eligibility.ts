import { AppError } from '../../../../shared/errors/app-error.js'; import { isUuid } from '../../../../shared/id/uuid.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { ControlledContext, EquipmentContext } from '../../../laboratory/domain/lab-test.js'; import type { CalibrationRecord } from '../../calibration/domain/calibration.js'; import type { Equipment } from '../domain/equipment.js'; import { isCalibrationOverdue } from '../../calibration/domain/calibration.js'; import type { EquipmentEligibility } from '../ports/equipment-eligibility.js';
export interface EquipmentEligibilityReader { getEquipment(id: string): Promise<Equipment | undefined>; getCalibration(id: string): Promise<CalibrationRecord | undefined>; }
export class GetEquipmentEligibilityUseCase implements EquipmentEligibility { constructor(private readonly reader: EquipmentEligibilityReader, private readonly now = () => new Date()) {} async verify(input: { actor: ActorContext; equipment: readonly EquipmentContext[]; context: ControlledContext }): Promise<void> { for (const usage of input.equipment) { if (!isUuid(usage.equipmentId) || !isUuid(usage.calibrationRecordId) || Number.isNaN(new Date(usage.usedAt).getTime())) throw new AppError('AUTHZ_DENIED', { userSafe: true, messageKey: 'assets.eligibility.invalidContext' }); const equipment = await this.reader.getEquipment(usage.equipmentId); const calibration = await this.reader.getCalibration(usage.calibrationRecordId); if (!equipment || !calibration || calibration.equipmentId !== equipment.id || equipment.state !== 'ACTIVE' || calibration.state !== 'CURRENT') throw new AppError('AUTHZ_DENIED', { userSafe: true, messageKey: 'assets.eligibility.invalidContext' }); if (isCalibrationOverdue(calibration, this.now())) throw new AppError('AUTHZ_DENIED', { userSafe: true, messageKey: 'assets.eligibility.overduePolicyUnresolved' }); if (!Object.keys(usage.equipmentSnapshot).length || usage.equipmentSnapshot.equipmentId !== equipment.id || !Object.keys(usage.calibrationSnapshot).length || usage.calibrationSnapshot.calibrationRecordId !== calibration.id) throw new AppError('AUTHZ_DENIED', { userSafe: true, messageKey: 'assets.eligibility.snapshotMismatch' }); } } }

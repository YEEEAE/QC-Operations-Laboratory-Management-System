import { authorize } from '../../../../shared/authorization/authorize.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { EquipmentRepository, EquipmentListFilter } from '../ports/repository.js';
export class ListEquipmentUseCase { constructor(private readonly repository: EquipmentRepository) {} execute(input: { actor: ActorContext; filter?: EquipmentListFilter }) { authorize({ actor: input.actor, permission: 'PERM-EQP-VIEW', action: 'VIEW', entity: { type: 'EQUIPMENT', id: 'list', state: input.filter?.state ?? 'DRAFT', ownerId: input.actor.id }, scope: { ownerId: input.actor.id }, currentVersion: 1n, expectedVersion: 1n, businessCondition: true }, { throwOnDeny: true }); return this.repository.list(input); } }

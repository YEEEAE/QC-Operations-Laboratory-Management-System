import { AppError } from '../../../../shared/errors/app-error.js'; import { authorize } from '../../../../shared/authorization/authorize.js'; import type { ActorContext } from '../../../../shared/authorization/types.js'; import type { EquipmentRepository } from '../ports/repository.js';
export class GetEquipmentUseCase { constructor(private readonly repository: EquipmentRepository) {} async execute(input: { actor: ActorContext; equipmentId: string }) { const equipment = await this.repository.get(input.equipmentId, input.actor); if (!equipment) throw new AppError('RESOURCE_NOT_FOUND', { userSafe: true }); authorize({ actor: input.actor, permission: 'PERM-EQP-VIEW', action: 'VIEW', entity: { type: 'EQUIPMENT', id: equipment.id, state: equipment.state, ownerId: equipment.createdBy }, scope: { ownerId: equipment.createdBy }, currentVersion: equipment.version, expectedVersion: equipment.version, businessCondition: true }, { throwOnDeny: true }); return equipment; } }
